services:
  frontend:
    container_name: nextjs-frontend
    build:
      context: .
      dockerfile: Dockerfile
    ports:
      - "443:443"
    restart: unless-stopped
    environment:
      NODE_ENV: production